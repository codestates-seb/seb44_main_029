cloud:
  aws:
    credentials:
      accessKey: ${AWS_ACCESS_KEY}
      secretKey: ${AWS_SECRET_KEY}
    s3:
      bucket: cozystates-bucket-01
    region:
      static: ap-northeast-2
    stack:
      auto: 'false'
spring:
  datasource:
#    url: jdbc:mysql://database-cozystates.c1hypxzbotyv.ap-northeast-2.rds.amazonaws.com:13306/cozystates?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    url: jdbc:mysql://localhost:3306/cozystates?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: Dong
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true  # (3) SQL pretty print
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
    sql:
      init:
        data-locations: classpath*:db/h2/data.sql
  logging:
    level:
      org:
        springframework:
          orm:
            jpa: DEBUG
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 492169011912-1o9evmkdle898mg5facghqgipvi415en.apps.googleusercontent.com #${CLIENT_ID}
            client-secret: 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 #${CLIENT_SECRET}
            scope:
              - profile
              - email
config:
  domain: http://cozystates-bucket-01.s3-website.ap-northeast-2.amazonaws.com
jwt:
  secret: dmskamdskamdksalndsanjfkqwgefuinklsjfajdlshuwhuqgbkbdasndksajugfekfmaldbqanbd #${JWT_SECRET}
  access_token_expired: 3_600_000 #3_600_000
  refresh_token_expired: 604_800_000

